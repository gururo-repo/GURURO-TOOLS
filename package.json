{
  "name": "tools-gururo-monorepo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "echo '🚀 MAIN BUILD SCRIPT RUNNING - v2.0' && node build-vercel.js",
    "dev": "cd apps/landing && npm run dev",
    "preview": "cd apps/landing && npm run preview"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
